import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        textAlign: 'center',
        paddingBottom: 5,
        marginHorizontal: 10,
        borderBottomWidth: 1,
    }
})